import React from 'react';

import './shared-utils.scss';

/* eslint-disable-next-line */
export interface SharedUtilsProps {}

export const SharedUtils = (props: SharedUtilsProps) => {
  return (
    <div>
      <h1>Welcome to shared-utils component!</h1>
    </div>
  );
};

export default SharedUtils;
